# src/

Place all your source code files here.

Suggested structure (update as you build):

```
src/
├── blocking.py        # Blocking stage: generates candidate_pairs.tsv
├── matching.py        # Matching stage: generates matching_results.tsv
├── features.py        # Feature engineering helpers
├── utils.py           # Shared utilities
└── config.py          # Hyperparameters and path configs
```
